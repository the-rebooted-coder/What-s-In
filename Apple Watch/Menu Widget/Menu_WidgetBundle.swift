import WidgetKit
import SwiftUI

@main
struct Menu_WidgetBundle: WidgetBundle {
    var body: some Widget {
        // This must match the struct name in Menu_Widget.swift
        Menu_Widget()
    }
}
